---
'@acusti/css-value-input': patch
---

Compile with React Compiler

This package’s build now runs through React Compiler, so its internals are
automatically memoized for better runtime performance. This is its first
published release built with the compiler. No API or behavior changes.
